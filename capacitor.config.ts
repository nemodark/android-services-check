import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.richard.app',
  appName: 'vue-first-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
